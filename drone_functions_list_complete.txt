# =================================================
# รายการฟังก์ชันที่ใช้ได้ใน drone_controller.py
# อัปเดตล่าสุด: 2024 (Complete Version - 80+ Functions)
# =================================================

## 📋 การเชื่อมต่อและจัดการ (Connection & Management)

1. **__init__(use_simulation=True)** - เริ่มต้น Drone Controller
2. **disconnect()** - ✅ ตัดการเชื่อมต่อ, ลงจอดก่อนถ้ายังบินอยู่, รีเซ็ตลม, เรียกใช้ drone_controller.disconnect()
3. **stop_simulation()** - หยุด CoppeliaSim simulation
4. **get_battery()** - ดูระดับแบตเตอรี่ (0-100% หรือ -1)

## 🚁 การบินพื้นฐาน (Basic Flight)

5. **takeoff(height=100)** - ขึ้นบิน (height: cm, default=100)
6. **land()** - ลงจอด
7. **hover(duration)** - โฮเวอร์เป็นเวลา duration วินาที

## 🧭 การเคลื่อนที่ (Movement)

### การเคลื่อนที่แบบสัมพัทธ์ (หน่วย: cm)
8. **move_forward(distance)** - เคลื่อนที่ไปข้างหน้า
9. **move_back(distance)** - ✅ เคลื่อนที่ไปข้างหลัง (ทั้ง sim & real)
10. **move_left(distance)** - เคลื่อนที่ไปทางซ้าย
11. **move_right(distance)** - เคลื่อนที่ไปทางขวา
12. **move_up(distance)** - เคลื่อนที่ขึ้น
13. **move_down(distance)** - เคลื่อนที่ลง

### การหมุน
14. **rotate_clockwise(degrees)** - หมุนตามเข็มนาฬิกา
15. **rotate_counter_clockwise(degrees)** - หมุนทวนเข็มนาฬิกา

### การเคลื่อนที่แบบแน่นอน
16. **move_to_position(x, y, z)** - เคลื่อนที่ไปยังตำแหน่งเฉพาะ
17. **get_position()** - ดึงตำแหน่งปัจจุบัน [x, y, z]
18. **get_orientation()** - ดึงทิศทาง (heading, roll, pitch, yaw)
19. **update_orientation()** - อัปเดต orientation ปัจจุบัน

## � กล้องและการถ่ายรูป (Camera & Photography)

### การถ่ายรูป
20. **capture(count=3, delay=1.5)** - ถ่ายรูปหลายรูป (ทั้งซิมและโดรนจริง)
21. **take_bottom_picture()** - ถ่ายรูปด้วยกล้องล่าง (simulation)

### การเปิด/ปิดกล้อง (เฉพาะโดรนจริง - DroneTello class)
22. **start_camera_display()** - เปิดแสดงภาพกล้อง
23. **stop_camera_display()** - ปิดแสดงภาพกล้อง
24. **capture(filename="tello_picture.jpg")** - ถ่ายรูปและบันทึก
25. **scan_qr(filename)** - แสกน QR จากไฟล์
26. **wait(seconds)** - รอเวลาพร้อมข้อความ
27. **cleanup()** - ทำความสะอาดก่อนปิด

### DroneCamera class (Simulation)
28. **simcapture(timeout=5.0)** - ถ่ายภาพจากกล้องหน้า
29. **simcapturebottom(timeout=5.0)** - ถ่ายภาพจากกล้องล่าง

## 🔍 การแสกน QR Code (QR Code Scanning)

30. **scan_qr(image_path=None)** - แสกน QR Code จากภาพ

### QRCodeScanner class
31. **scan_qr_code(image_path)** - แสกน QR Code
32. **draw_qr_detection(image_path, output_path=None)** - วาดกรอบรอบ QR Code

## 🎯 Mission Pad Detection

### การเปิด/ปิดระบบ
33. **enable_mission_pads()** - เปิดใช้งาน Mission Pad detection
34. **disable_mission_pads()** - ปิด Mission Pad detection

### การตรวจจับ
35. **get_mission_pad_id()** - ดึง Mission Pad ID (ต้องเรียกใช้ด้วยตนเอง)
36. **scan_mission_pad()** - แสกน Mission Pad
37. **smart_mission_pad_scan(image_path=None, use_multiple_methods=True)** - ตรวจจับขั้นสูง
38. **scan_mission_pad_enhanced(attempts=3, delay=1.0)** - ตรวจจับแบบลองหลายครั้ง

### การตั้งค่าและทดสอบ
39. **set_mission_pad_confidence(threshold)** - กำหนดค่าความเชื่อมั่น
40. **get_mission_pad_templates_info()** - ดูข้อมูล templates
41. **test_mission_pad_detection(test_image_path)** - ทดสอบด้วยรูปภาพ
42. **test_mission_pad_detection_simple()** - ทดสอบแบบง่าย
43. **debug_mission_pad_system()** - ตรวจสอบระบบทั้งหมด
- scan_qr_code(image_path) # แสกน QR Code จากไฟล์ภาพ
- draw_qr_detection(image_path, output_path=None) # วาดกรอบ QR Code

#### Integrated QR Functions
- scan_qr(filename) # แสกน QR Code (real drone)
- scan_qr(image_path=None) # แสกน QR Code (simulation)

### 🎯 MISSION PAD FUNCTIONS

#### Basic Mission Pad
- enable_mission_pads() # เปิดใช้งาน mission pad detection
- disable_mission_pads() # ปิดใช้งาน mission pad detection
- scan_mission_pad() # แสกน mission pad
- get_mission_pad_id() # อ่าน mission pad ID
- set_mission_pad_confidence(threshold) # ตั้งค่าความมั่นใจ

#### Enhanced Mission Pad
- scan_mission_pad_enhanced(attempts=3, delay=1.0) # แสกนแบบปรับปรุง
- smart_mission_pad_scan(image_path=None, use_multiple_methods=True) # แสกนอัจฉริยะ
- test_mission_pad_detection(test_image_path) # ทดสอบการตรวจจับ
- test_mission_pad_detection_simple() # ทดสอบแบบง่าย
- get_mission_pad_templates_info() # ข้อมูล templates
- debug_mission_pad_system() # ดีบัก mission pad system

### 📊 SENSOR & STATUS FUNCTIONS

#### Position & Orientation
- get_position() # อ่านตำแหน่งปัจจุบัน
- get_orientation() # อ่านการหันหน้า
- update_orientation() # อัพเดทการหันหน้า

#### Battery & Status
- get_battery() # อ่านแบตเตอรี่ (%)

#### Proximity Sensors (ProximitySensorManager)
- read_distance() # อ่านระยะทาง proximity sensor
- get_height() # อ่านความสูง
- get_altitude() # อ่านระดับความสูง
- is_close_to_ground(threshold=0.3) # ตรวจสอบใกล้พื้น
- is_safe_altitude(min_height=0.3) # ตรวจสอบความสูงปลอดภัย
- read_proximity_sensor(sensor_name='bottom') # อ่าน sensor เฉพาะ
- get_sensor_info() # ข้อมูล sensor ทั้งหมด

#### Advanced Altitude Control
- wait_until_height(target_height, timeout=10) # รอจนถึงความสูง
- monitor_height(duration=5) # ติดตามความสูง
- auto_maintain_altitude(target_height=1.0, tolerance=0.1, max_time=30) # รักษาความสูงอัตโนมัติ
- smart_landing(check_interval=0.5, safe_height=0.15) # ลงจอดอัจฉริยะ

### 🌪️ WIND SIMULATION FUNCTIONS

#### Wind System Setup
- setup_wind_system() # ตั้งค่าระบบลม
- set_wind_strength(strength) # ตั้งความแรงลม
- set_wind_direction(x=0, y=0, z=0) # ตั้งทิศทางลม
- enable_turbulence(enable=True) # เปิด/ปิด turbulence
- enable_wind_gusts(enable=True) # เปิด/ปิด wind gusts

#### Wind Presets
- set_calm_conditions() # สภาพอากาศสงบ
- set_light_breeze() # ลมเบาๆ
- set_moderate_wind() # ลมปานกลาง
- set_strong_wind() # ลมแรง
- create_realistic_wind_scenario() # สร้างสถานการณ์ลมจริง

#### Wind Zone & Testing
- create_wind_zone(name, x_min, y_min, x_max, y_max, wind_multiplier=1.0, turbulence_level=0.1) # สร้างเขตลม
- get_wind_status() # สถานะลมปัจจุบัน
- test_wind_effects_simple() # ทดสอบผลกระทบของลม
- start_wind_demo() # เริ่มการสาธิตลม

### 🔧 UTILITY FUNCTIONS

#### Wait & Control
- wait(seconds) # รอเป็นวินาที (real drone)

#### Safety & Monitoring
- monitor_flight_safety(min_altitude=0.3, max_altitude=3.0, check_interval=1.0) # ติดตามความปลอดภัย
- scan_surroundings_with_altitude(points=8) # สแกนรอบด้านพร้อมความสูง

## 📝 หมายเหตุการใช้งาน

### Real Drone (DroneTello)
- ระยะทางใช้หน่วย: เซนติเมตร (cm)
- มุมใช้หน่วย: องศา (degrees)
- ฟังก์ชัน move_back() สำหรับเคลื่อนที่ถอยหลัง

### Simulation Drone (NaturalDroneController)
- ระยะทางใช้หน่วย: เมตร (meters)
- มุมใช้หน่วย: องศา (degrees)
- ฟังก์ชัน move_backward() สำหรับเคลื่อนที่ถอยหลัง
- รองรับระบบลมและ proximity sensors

### ตัวอย่างการใช้งาน

```python
# Simulation
drone = NaturalDroneController(use_simulation=True)
drone.takeoff()
drone.move_forward(1.0) # 1 เมตร
drone.rotate_clockwise(90)
drone.land()

# Real Drone
drone = DroneTello()
drone.takeoff(100) # ขึ้น 100 cm
drone.move_forward(100) # เคลื่อนที่ 100 cm
drone.rotate_clockwise(90)
drone.land()
```

## 🔄 อัพเดท
- ✅ เพิ่มฟังก์ชันใหม่ทั้งหมดจาก drone_controller.py
- ✅ แยกประเภทตามการใช้งาน
- ✅ ระบุความแตกต่างระหว่าง Real Drone และ Simulation
- ✅ เพิ่มตัวอย่างการใช้งาน

## 📚 CLASSES ที่สำคัญ

### 1. NaturalDroneController
- **วัตถุประสงค์**: ควบคุมโดรนใน CoppeliaSim simulation
- **พารามิเตอร์**: use_simulation=True
- **ความสามารถ**: ระบบลม, proximity sensors, mission pads
- **หน่วย**: เมตร, องศา

### 2. DroneTello (extends Tello)
- **วัตถุประสงค์**: ควบคุมโดรน DJI Tello จริง
- **พารามิเตอร์**: show_cam=False, enable_mission_pad=False
- **ความสามารถ**: กล้องจริง, บินจริง, mission pads
- **หน่วย**: เซนติเมตร, องศา

### 3. DroneCamera
- **วัตถุประสงค์**: จัดการกล้องใน simulation
- **ความสามารถ**: ถ่ายภาพ simcapture(), simcapturebottom()

### 4. QRCodeScanner
- **วัตถุประสงค์**: แสกน QR Code จากภาพ
- **ความสามารถ**: scan_qr_code(), draw_qr_detection()

### 5. ProximitySensorManager
- **วัตถุประสงค์**: จัดการ proximity sensors ใน simulation
- **ความสามารถ**: วัดระยะทาง, ความสูง, ความปลอดภัย

### 6. ImprovedMissionPadDetector / MissionPadDetector
- **วัตถุประสงค์**: ตรวจจับ mission pads
- **ความสามารถ**: แสกน mission pad templates

## 🎯 COMPATIBILITY NOTES

### GUI Connector ที่ใช้ได้
เนื่องจาก `drone_gui_connector.py` ได้รับการปรับปรุงให้เรียกใช้ฟังก์ชันโดยตรง:

```python
# ใน GUI Connector
if self.drone_mode == "simulation":
success = self.current_drone.move_forward(distance) # เมตร
elif self.drone_mode == "real":
self.current_drone.move_forward(distance_cm) # cm
```

### ฟังก์ชันที่เหมือนกัน
- **ทั้ง Real Drone และ Simulation**: `move_back()` สำหรับถอยหลัง

### ฟังก์ชันเฉพาะ
- **Real Drone**: หน่วยเป็น cm, takeoff(height), capture(filename)
- **Simulation**: หน่วยเป็น m, simcapture(), ระบบลม, proximity sensors

## 🚨 สำคัญ!
หากมีการเปลี่ยนแปลงชื่อฟังก์ชันใน `drone_controller.py` ให้อัพเดทไฟล์นี้ด้วย